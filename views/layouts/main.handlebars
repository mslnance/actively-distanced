<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Actively Distanced</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
</head>

<body>
    <header>
        <nav class="navbar navbar-default">
            <a class="navbar-brand" href="/">Actively Distanced</a>
        </nav>
    </header>


    <!-- Navbar Section -->
    <section class="nav-sec">
        <div class="d-flex justify-content-between">
            <div class="p-2 nav-icon-lg mint-green">
                <a class="nav-icon" href="/">
                    <h2 class="ui white image header">Home</h2>
                </a>
            </div>
            {{#if loggedIn}}
            <div class="p-2 nav-icon-lg mint-green">
                <a class="nav-icon" href="/create-post">
                    <h2 class="ui white image header">+ Activity</h2>
                </a>
            </div>
            <div class="p-2 nav-icon-lg mint-green">
                <a class="nav-icon" href="/my-activities">
                    <h2 class="ui white image header">My activities</h2>
                </a>
            </div>
            <div class="p-2 nav-icon-lg mint-green">
                <a class="nav-icon" href="/logout">
                    <h2 class="ui white image header">Logout</h2>
                </a>
            </div>
            {{else}}
            <div class="p-2 nav-icon-lg mint-green">
                <a class="nav-icon" href="/login">
                    <h2 class="ui white image header">Login</h2>
                </a>
            </div>
            {{/if}}
        </div>
    </section>

    <div class="wrapper">

        <main>
            {{{ body }}}
        </main>
        <footer>
            <center>
                <h4 class="ui teal image header">Have fun with safe activities! </h4>
            </center>
        </footer>
    </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>